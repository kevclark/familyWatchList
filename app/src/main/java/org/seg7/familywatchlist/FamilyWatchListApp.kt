package org.seg7.familywatchlist

import android.app.Application

/**
 * Application entry point.
 *
 * M1 attaches the manual DI container here (PLAN.md §1: single [AppContainer], no Hilt).
 */
class FamilyWatchListApp : Application()
